// import express from 'express';
// import { loginAdmin } from '../controllers/adminController';
// import { verifyWebhook } from '../controllers/webhookController';  // Ensure it's imported correctly
// import { getMessages, updateMessages, sendMessage } from '../controllers/messageController';
// import { authenticateToken } from '../controllers/authController';

// const router = express.Router();

// router.post('/admin/login', loginAdmin);
// router.get('/webhook', verifyWebhook);

// router.get('/admin/messages', authenticateToken, getMessages);
// router.post('/admin/update-messages', authenticateToken, updateMessages);
// router.post('/admin/send-message', authenticateToken, sendMessage);

// export default router;
